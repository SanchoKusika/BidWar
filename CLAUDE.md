# BidWar — рабочие правила

Telegram Mini App: платный и бесплатный топы проектов. Позиция в платном покупается
(Raise) и отбирается (Attack), в бесплатном зарабатывается голосами за задания.

## Прежде чем начать задачу

**Спецификация лежит вне репозитория,** в Obsidian: `~/Documents/Obsidian Storage/Проекты/BidWar/`
(13 файлов, `00 Обзор` … `12 Состояние реализации`). Требования брать оттуда, а не
восстанавливать по коду.

Читать в таком порядке:

1. `12 Состояние реализации` — **что реально работает, а что только нарисовано.** Здесь же
   список известных мелких дефектов, чтобы не искать их заново.
2. `08 План разработки` — порядок срезов и что делает текущий.
3. Профильный документ под задачу: `01 Механики`, `03 Данные`, `04 Платежи и валюты`,
   `05 Аккаунты и авторизация`, `06 Telegram-бот и Mini App`.

Если документ разошёлся с кодом — правда в коде, а документ надо починить тем же
заходом. Расхождение, оставленное на потом, обходится дороже правки.

## Состояние (сверять с документом 12, здесь только ориентир)

Закрыты срезы 1.1–1.9 плюс полный перенос вёрстки из дизайн-кита. 1.8 и 1.9
закрыты 11.09.2026 (PR #41–#44 и #46, #47): профиль и настройки сведены с китом,
интерфейс заговорил на трёх языках, чеки показывают списанную сумму в валюте
списания, бот пишет по четырём событиям. Следующий и последний в Этапе 1 —
**1.10: реальный провайдер**.

**Уведомления не участвуют в денежном пути.** Их кладут в очередь
(`notifications`) четыре триггера, читающие уже записанный леджер; ни
`apply_payment`, ни `cast_votes`, ни `apply_task_completion` про них не знают, и
тело каждого триггера завёрнуто в `exception when others` — непосланное
сообщение это неприятность, откаченная оплата это деньги. Агрегация — частичный
уникальный индекс на неотправленные строки, а не логика отправщика. Разбирает
очередь функция `notify`, которую раз в минуту будит `pg_cron` через `pg_net`;
её секрет и адрес лежат в Vault (как заводить — в README).

**Платный вход, Raise и Attack работают на моке.** `apply_payment` —
единственная хранимка, двигающая деньги и очки, идемпотентна, блокирует строки
по возрастанию `id`; Attack живёт внутри неё же. Raise работает и по чужому
проекту — это донат в его ставку, а не перевод владельцу (`01 Механики`).

**Голоса устроены так же, но своей парой хранимок:** `apply_task_completion` —
единственное место, где голоса появляются (реферальная доплата внутри неё же,
за первое выполненное задание приглашённого), `cast_votes` — единственное, где
они тратятся. Отдавать голоса чужому проекту можно, проверки владения нет
нигде по пути. Невёрстанных механик в Этапе 1 не осталось.

Настоящий рубеж между тестовым контуром и боевым — переменная окружения
Edge Functions `PAYMENT_PROVIDER=mock`, а не флаг `PREVIEW.mockPayments`:
последний красит только список способов оплаты в `PaySheet` (найдено
финальным ревью 1.5 — молчаливый дефолт `?? 'mock'` в `registry.ts` делал
незаданную переменную неотличимой от мока; дефолта больше нет, `getProvider()`
бросает, если переменная не задана). Платежи пока идут через мок, платное #1
можно занять бесплатно. **`PAYMENT_PROVIDER` обязан смотреть на настоящего
провайдера до боевого запуска** — это первое, что проверяют в чек-листе
запуска, флаг `PREVIEW.mockPayments` снимается тем же заходом, но он
косметический.

**«Удалить мои проекты» в настройках — инструмент разработки** (12.09.2026).
Хранимка `reset_user_data` стирает проекты, платежи, ставки, голоса и задания
аккаунта по-настоящему: переписывает леджер, сбрасывает лимиты атак и даёт
заново получать награды — для тестового контура это принято осознанно. Держат
её в тестовом контуре два замка: `remove-my-projects` отвечает 404, если
`PAYMENT_PROVIDER` не `mock`, а строка видна только под `PREVIEW.mockPayments`.
У пользователей боевой версии такой кнопки не будет — это действие админ-панели.

**Тесты есть, раннер `deno test`.** `npm run test` — 118 быстрых тестов без базы,
входит в `npm run check` и в CI. `npm run test:db` — интеграционные тесты
хранимок против живой базы, требует `SUPABASE_DB_URL`, в `check` и CI не
входит намеренно.

## Git

- Conventional Commits, **сообщения на английском**, тело только когда изменение
  правда требует объяснения.
- **Никогда не добавлять трейлер `Co-Authored-By`.**
- Не работать напрямую в `main`: ветка под кусок работы → коммиты → `gh pr create` →
  мерж. Squash-мерж, ветку удалять.
- Историю не переписывать. Шесть старых коммитов с русскими сообщениями оставлены
  намеренно, force-push в `main` не делать.

## Перед пушем

```bash
npm run check     # типы + линт + prettier + изоляция платформы + npm run test
```

Состав **намеренно совпадает с CI**. Добавляешь проверку в пайплайн — добавь и в
`check`, иначе расхождение всплывёт уже красным после пуша (так и случилось однажды
с `format:check`).

`npm run test` (`deno test`, нужен установленный Deno) гоняет тесты Edge Functions —
без сети и без базы. Отдельно, руками, перед пушем миграции, трогающей хранимки:
`npm run test:db` — интеграционные тесты против **живой** базы, требует
`SUPABASE_DB_URL` в `.env`. В `check` и CI намеренно не входит.

## Правила кода

**Структура — FSD.** Слой ниже не знает про слой выше: `app > pages > widgets >
features > entities > shared`. Нарушение ловит `no-restricted-imports` в
`eslint.config.js` для `entities`, `features`, `shared`, `pages/*/model`.

**Ничто вне `shared/platform` и `app/telegram` не трогает `window.Telegram`.**
Проверяется `scripts/check-platform-isolation.sh` в CI. Это единственное, что
удерживает возможность сделать веб в Этапе 2, не переписывая фичи.

**Только CSS-модули, значения из токенов** (`shared/ui/tokens/*.css`). Инлайн-стиль
допустим единственным способом — пробросить динамическое значение в CSS-переменную:

```tsx
style={{ '--og-size': `${size}px` } as CSSProperties}
```

Захардкоженных цветов вне `tokens/` в проекте нет — так и держать.

**Бизнес-правила живут на сервере.** Лимиты ставок и коэффициенты атаки — в
`app_config.paid_limits`, клиент читает их через `fetchPaidLimits` и использует
**только для предпросмотра**; окончательный расчёт делает сервер. Дублировать эти
числа константами в клиенте нельзя — расхождение здесь стоит денег.

**Блоки без источника данных — за флагами** в `shared/config/preview.ts`. По этому
файлу видно границу между витриной и работающей функцией. Подключение фичи — это
убрать флаг вместе с его ветками.

Флаг — не единственный способ: где источник приходит пропом, честнее обойтись
без него. `ShowcaseScreen` рисует ленту событий и переключатель «All time /
Today», только если страница передала им данные. У бесплатного топа их не было
до 1.7, и блоки там просто отсутствовали — а появились сами, как только
страница начала передавать `free_today_top` и `vote_activity`: в самом
`ShowcaseScreen` менять не пришлось ни строчки.

Если элемент выглядит рабочим, но ничего не делает, — его надо **прятать, а не
дизейблить**: неработающая кнопка врёт пользователю сильнее, чем отсутствующая.
Так была спрятана кнопка «поделиться» без `onShare`, и так же жили тумблеры
уведомлений, пока рассылки не было.

**Спрятать и удалить — разные решения.** За флагом лежит то, что будет
написано, — и хорошо видно, чем такой флаг кончается: `settingsStubs` прятал
язык (пришёл в 1.8) и уведомления (пришли в 1.9), после чего исчез вместе с
ветками. То, чего в продукте не будет вовсе, удаляется вместе со строками в
`i18n`, а не прячется: так из настроек ушли «подтверждать каждый платёж»,
«выйти» и «удалить аккаунт» (10.09.2026), а следом «новые задания» — объявлять
в фиксированном списке заданий нечего (11.09.2026). Флаг показывает, **что
придёт**; держать в нём то, что не придёт, — врать самим себе, и через полгода
одно от другого уже не отличить.

**Число всегда идёт со своей единицей, и единица не врёт.** Внутри всё считается
в очках (1 очко = 1 сум), на экран число выходит **в валюте зрителя**
(`04 Платежи и валюты`). Поменять подпись, не пересчитав число, — готовый баг:
ровно так поле ввода показывало «50 000 $» вместо «4.13 $». Исключение —
чек и история платежей: там показывается ровно то, что списано, и это не
пересчитывается никогда.

**Витрины ходят через кэш** `shared/lib/query.ts` (stale-while-revalidate).
Отсюда два разных флага вместо одного: `loading` — показывать нечего, уместен
скелетон; `refreshing` — на экране прошлый ответ, свежий в пути. Там, где ждут
именно свежие данные (ранг в квитанции после оплаты), проверять надо
`refreshing`.

**Заглушка повторяет геометрию содержимого.** Иначе она не убирает прыжок, а
переносит его на момент подмены: страница проекта показывала три строки ленты
и меняла их на шапку с плитками. Отсюда же три следствия: место под блок,
который приедет позже, держится с первого кадра; у экрана с несколькими
источниками флаг загрузки на каждый ответ, а не один общий; заглушка внутри
flex-строки обязана нести собственный `flex`, иначе она схлопывается в ноль
ширины и место оказывается зарезервированным только в коде.

Геометрию не подбирают, а берут у компонента (12.09.2026): заглушка живёт
рядом с ним и рисуется его же классами (`ProjectCardSkeleton`,
`TaskListItemSkeleton`, `CategoryTileSkeleton`, `ProjectScreenSkeleton`,
`loading` у `StatBlock` и панелей). На месте неизвестного значения —
`SkeletonText`: текст в шрифте этого слота, поэтому высота строки та же, что у
числа, которое придёт; ширину задаёт образец значения («000 000»). Процент
ширины годится только в блочном родителе — у flex-элемента, сжатого по
содержимому, он даёт ноль. Фиксированные формы — `SkeletonBlock` с размером из
CSS компонента. Прямоугольник с высотой «на глаз» расходится с блоком при
первой же правке типографики. И пока сессия в пути, у запросов нет ключа и они
сообщают «не грузится» — это тоже «ответа ещё нет», а не пустота.

**Пустое состояние — не то же самое, что «ответа ещё нет».** «У тебя нет
проектов» до ответа сервера это утверждение, которого мы не знаем. И наоборот:
`null` в данных бывает конечным состоянием — у гостя без `initData` баланс
голосов пуст навсегда, и вечная заглушка на его месте врала бы про загрузку,
которой нет.

Ключ общий — значит общий во все стороны (11.09.2026): полученный ответ
рассылается всем хукам на этот ключ, а первый запрос у них один на всех. До
этого каждый хук жил со своей копией, и счётчик заданий в меню замирал до
перезапуска, пока экран заданий обновлял тот же ключ. Явный `refresh()` из
общего запроса выходит намеренно: его зовут ровно тогда, когда прошлый ответ
уже не годится.

## Ловушки, на которые уже наступали

**`react-hooks/set-state-in-effect`** — нельзя вызывать `setState` синхронно в теле
эффекта. Сброс состояния при смене пропа делается сравнением в рендере с зеркалом в
`useState`:

```tsx
const [loadedId, setLoadedId] = useState(id);
if (loadedId !== id) {
  setLoadedId(id);
  setStatus('loading');
}
```

**`react-hooks/refs`** — не мутировать `.current` во время рендера.

**`noUncheckedIndexedAccess: true`** — обращение по индексу даёт `T | undefined`.
Для гарантированно непустых списков объявлять непустой кортеж:
`readonly [PaymentProvider, ...PaymentProvider[]]`.

## Язык

**Всё, что уходит в репозиторий, пишется по-английски:** сообщения коммитов,
описания pull request'ов и комментарии в коде. Решено 02.09.2026 — до этого
комментарии и PR-описания были русскими.

Исключения, и они не про язык, а про адресата:

- **спецификация в Obsidian** — документ автора, остаётся русской;
- **`README.md` и этот файл** — написаны по-русски и переводятся не по одной
  фразе, а отдельным заходом, если он понадобится;
- **уже написанные русские комментарии** не переводятся задним числом:
  правишь функцию — комментарий рядом становится английским, остальное
  не трогаешь. Смешанный код на время миграции — меньшее зло, чем диффы,
  в которых правка утонула среди переводов.

**Интерфейс говорит на трёх языках.** `shared/i18n`: `en.ts` задаёт форму
словаря и обязан быть полным, `ru.ts` и `uz.ts` частичные — непереведённый ключ
падает на английский, а не на пустоту или имя ключа. Новая строка заводится в
английском, иначе её не увидит ни один словарь. Обращаться к строкам только
через `strings` из `shared/i18n/strings` — это прокси, читающий язык в момент
обращения; снимок `const t = strings.tasks` на уровне модуля жив и переживает
переключение языка, а свой объект словаря — нет. Захардкоженного текста в
компонентах быть не должно: он не переводится и выдаёт два языка на экране.

**`shared/content`** (правила игры, правовые страницы, подписи провайдеров)
переведён тоже, но устроен иначе: по файлу на язык и геттеры `getRules()`,
`getDocs()`, `getPaymentMethods()`. Читать их только вызовом и только в
отрисовке — список, собранный на уровне модуля, застынет на языке первого
запуска. Перевод здесь **всегда целиком документа**: словарь может падать на
английский по одному ключу, правила — нет, половина раздела на чужом языке хуже
целого.

Что остаётся английским намеренно: пилюля `PAID`/`FREE` в шапке (она называет
топ, а не читается словом), имена провайдеров и названия карт (Uzcard, Humo),
названия заданий в БД — их подписи печатает клиент по типу задания
(`entities/task/taskCopy`), а из базы берёт только награду и состояние.

**Суммы в сообщениях бота живут отдельно** — `supabase/functions/_shared/
notify_text.ts`: у Edge Functions другой рантайм, клиентский `shared/i18n` туда
не импортируется. Дублирование осознанное и маленькое, четыре сообщения.

Комментарий объясняет **почему**, а не что: «что» видно из кода.

## Инфраструктура

- **Edge Functions деплоятся руками**, в CI их нет: `npm run functions:deploy`
  выкладывает все разом. `verify_jwt` каждой прописан в `supabase/config.toml` —
  без него массовый деплой включит проверку JWT двум вебхукам (`bot`,
  `payment-webhook`), которых зовут без заголовка `Authorization`. Разъехавшийся
  деплой уже стоил среза: в проде висела `create-payment` от 1.5, и **любая
  атака отвечала ошибкой валидации**, хотя код в `main` был верный.
- Фронт выкладывается автоматически на каждый пуш (Cloudflare Pages).
- После миграции — `npm run db:types`, файл генерируемый. Проект не залинкован:
  `db:types` с `--linked` падает и **оставляет файл пустым** — генерировать через
  `--project-id`.
- **Миграция попадает в базу только файлом через `db push`.** `apply_migration` из
  MCP пишет в историю свою версию с текущим временем, и она расходится с файлом:
  12.09.2026 четыре такие записи пришлось снимать `migration repair --status
reverted` перед `db push`. Строку подключения `db push` берёт из
  `SUPABASE_DB_URL` (`--db-url`).
- Одно развёртывание отвечает на трёх хостах: `bidwar.world` (сайт, Этап 2),
  `app.bidwar.world` (мини-апп), `bidwar.pages.dev` (служебный алиас Pages, несносим).
  Индексируется только первый, остальные закрыты `X-Robots-Tag` в `public/_headers`.

Подробности стека, команд и деплоя — в `README.md`.
